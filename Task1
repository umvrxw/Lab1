#include <iostream>
#include <cmath>

int main() 
{
using namespace std;
    int x, y, e;
     cout << "x: " << endl;
    cin >> x;
     cout << "y: " << endl;
    cin >> y;
    e = 2 * (x - 1) * (1 + y);
     cout << e << endl;
//system("pause");
    return 0;
}
